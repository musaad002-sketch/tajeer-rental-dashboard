# الوصول العام إلى تأجيرك

## رابط الموقع

يفتح الموقع العام من النطاق المنشور:

`https://tajeerkapp-sr9sxgzl.manus.space/`

من خارج جهاز الخادم، يستخدم المستخدم هذا النطاق وليس `localhost`.

## إنشاء حساب مشغّل

ينشئ المدير الحساب من صفحة إدارة المستخدمين ويضيف بريداً إلكترونياً صحيحاً للمستخدم. يظهر الحساب بحالة بريد غير موثق إلى أن يتم تفعيل الرابط.

## إنشاء رابط التحقق

من صف المستخدم غير الموثق يضغط المدير على «إنشاء رابط تحقق» ثم ينسخ الرابط الناتج ويرسله يدوياً إلى البريد المسجل للمستخدم. الرابط صالح لمدة 24 ساعة ويستخدم مرة واحدة فقط؛ بعد فتحه تُحذف قيمة الرمز من الحساب.

## دخول المشغّل

بعد فتح الرابط بنجاح، يدخل المشغّل من النطاق العام باسم المستخدم وكلمة المرور المحليين. لا يسمح الموقع بالدخول العام للحساب قبل توثيق البريد، بينما يبقى `localhost` متاحاً للتشغيل المحلي والاختبار.

## ملاحظات الأمان

لا تُرسل رابط التحقق إلى شخص غير مالك البريد، ولا تُدرج كلمات المرور داخل الرسائل أو الملفات. إذا انتهت صلاحية الرابط، ينشئ المدير رابطاً جديداً من صفحة إدارة المستخدمين. تبقى صلاحيات المشغّل التشغيلية سارية، وتظل عمليات التعديل والحذف محصورة بالمدير.

## تشغيل محلي على المنفذ 8000

في Windows افتح PowerShell داخل مجلد المشروع ونفّذ:

```powershell
$env:PORT=8000
pnpm dev
```

ثم افتح `http://localhost:8000`. لا تشغّل Apache على المنفذ 8000 في XAMPP في الوقت نفسه؛ يكفي تشغيل MySQL من XAMPP. في Linux أو macOS يمكن استخدام `PORT=8000 pnpm dev`. إذا كان المنفذ مشغولاً، أوقف الخدمة التي تستخدمه أو اختر منفذاً آخر.
