# نتيجة فحص OAuth على الهاتف

قبل الإصلاح كان الرجوع من بوابة Manus يعرض `{"error":"invalid oauth state"}` على الهاتف.

بعد تعديل البداية، أصبح المتصفح يطلب `/api/oauth/start?origin=<window.location.origin>`، ويعيد الخادم تحويلاً إلى بوابة Manus مع `redirectUri` مطابق تماماً لأصل رابط المعاينة ومسار `/api/oauth/callback`. كما تظهر شاشة اختيار الحساب من Manus بصورة طبيعية.

لم يتم اختيار الحساب أو إتمام تسجيل الدخول آلياً لأن ذلك إجراء يتطلب موافقة المستخدم. ما زال يلزم اختبار الرجوع النهائي بعد اختيار الحساب على الهاتف نفسه.

## نتيجة الإصلاح الثاني

تم اختبار endpoint عبر HTTPS بعد التعديل. أعاد استجابة `302` إلى بوابة Manus، وكانت ترويسة `Set-Cookie` تحتوي على `HttpOnly; Secure; SameSite=None; Path=/; Max-Age=600`، كما حملت قيمة `redirectUri` أصل التطبيق نفسه. هذا يعالج سبباً شائعاً لفقدان cookie عند الرجوع من بوابة خارجية على متصفحات الهاتف، مع بقاء التحقق من nonce إلزامياً في callback.

يبقى الاختبار النهائي من الهاتف الحقيقي مهماً لأن المتصفح قد يمنع cookies أو يغيّر سلوك التتبع، ولأن إتمام callback يتطلب اختيار الحساب فعلياً.
