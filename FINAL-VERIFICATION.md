# تقرير التحقق النهائي — تأجيرك

## نطاق الإصلاح الحالي

تم تثبيت نموذج العميل على خانة واحدة لرقم الجوال الأساسي، مع إبقائه إلزامياً وإضافة نوع إدخال هاتف مناسب للآيفون. لم تُضف أي خانة جوال ثانية، ولم تتغير بنية بيانات العملاء الحالية التي تعتمد على العمود `customers.phone` الواحد.

تمت إضافة خانة **ملاحظات العقد** إلى نموذج إنشاء العقد، وتُحفظ في `contracts.notes` كما تُضمّن في أول سجل تاريخي للعقد. كما تمت إضافة خانة **ملاحظات العملية** إلى جميع فروع التشغيل: الدفعة، التمديد، تبديل السيارة، التعليق، الإغلاق، والاسترجاع. تُحفظ ملاحظات العمليات في `contractOperations.details`، وتُنسخ ملاحظات الدفعات إلى `payments.notes`، وتظهر في تفاصيل العقد وسجل العمليات مع التاريخ والوقت والمنفذ.

## نتائج التحقق الآلي

| الفحص | النتيجة |
|---|---|
| TypeScript (`pnpm check`) | ناجح |
| Vitest | 19 ملف اختبار، 61 اختباراً ناجحاً |
| البناء الإنتاجي (`pnpm build`) | ناجح |
| قاعدة البيانات | لا توجد migration جديدة؛ الحقول اللازمة موجودة مسبقاً |

ظهر تحذير حجم الحزمة من Vite فقط، دون فشل في البناء.

## الفحص البصري

تم فحص المسارات `/` و`/contracts/new` و`/customers` و`/contracts/active` و`/accounting` على مقاس **375×812** لمحاكاة iPhone. ظهرت النماذج باتجاه RTL، وبحقول لمس مناسبة، ودون ظهور خانة جوال مكررة في نموذج العميل. خانة الملاحظات في نموذج العقد ونماذج العمليات موضوعة أسفل الحقول التشغيلية للحفاظ على التسلسل الواضح.

## حدود القبول اليدوي

لا يمكن إثبات جلسة قبول تفاعلية كاملة داخل حساب المستخدم أو تشغيل سكربت الاستعادة فعلياً من بيئة Linux الحالية التي لا توفر PowerShell وMySQL المحليين الخاصين بجهاز Windows. لذلك يجب تنفيذ الخطوات التالية على جهاز المستخدم قبل الاعتماد التشغيلي النهائي: تسجيل الدخول المحلي، إنشاء عقد يومي وشهري، تسجيل دفعة كاش وشبكة مع ملاحظة، فتح التفاصيل، ثم تجربة التمديد والتبديل والتعليق والإغلاق والاسترجاع والتحقق من السجل.

## طريقة التشغيل المحلي

توجد إرشادات Windows في `README-Windows.md`، وملفات الإعداد والنسخ الاحتياطي والاستعادة في `setup-windows.ps1` و`setup-local-db.ps1` و`backup-local-db.ps1` و`restore-local-db.ps1`. لا تُشغّل الاستعادة إلا بعد أخذ نسخة احتياطية والتأكد من قيمة التأكيد المطلوبة داخل السكربت.

أُعد هذا التقرير بواسطة Manus AI في 28 أغسطس 2026.

## فحوص مساندة

تستخدم الواجهة تسميات مرئية و`aria-label` للأزرار والحقول التفاعلية، وتحافظ على حلقات التركيز، كما أن أزرار اللمس الأساسية لا تقل عن مساحة مناسبة للهاتف. تمت مراجعة مسارات localhost وفق مسار الدخول المحلي الموجود في المشروع؛ أما إثبات بيانات الاعتماد الفعلية فيتطلب تشغيل إعدادات Windows المحلية لدى المستخدم.

تمت مراجعة `restore-local-db.ps1` نصياً: يرفض اسم قاعدة غير آمن، يطلب التأكيد الصريح `RESTORE`، ينفذ إسقاطاً وإعادة إنشاء ثم يمرر ملف SQL إلى عميل MySQL مع فحص `$LASTEXITCODE`. لم يُنفذ الاسترجاع تدميرياً هنا لعدم توفر PowerShell وMySQL محليين.

تظل جلسة القبول التفاعلي ببيانات المستخدم على iPhone، بما فيها إنشاء عقد وتشغيل جميع الفروع، مسؤولية فحص يدوي قبل الاستخدام الإنتاجي.

## مرجع النسخة

نسخة المشروع المحفوظة: `09020db5`.

## الفحص البصري الإضافي للجداول واللوحات الحساسة

أُعيد فحص مسارات السيارات والصيانة والتقارير والمدفوعات والاسترجاعات والعقود المتأخرة والمعلقة ونموذج العقد الجديد على 375×812 بارتفاع كامل. ظهرت بطاقات العمليات وملاحظاتها، وجداول التقارير، وملخص إيراد السيارات، ونموذج العقد، دون تداخل بين العناوين والحقول أو قص لخانة الملاحظات. مسارا المدفوعات والاسترجاعات يعرضان حالة الدخول/السجل ضمن إطار RTL، مع ملاحظة أن الجداول العريضة تستخدم تمريراً أفقياً داخل الحاوية بدلاً من كسر الصفحة.

حزمة Windows الموجودة في ZIP هي **حزمة تجهيز للتنزيل والفحص المحلي** وليست اعتماداً نهائياً للتشغيل؛ الاعتماد النهائي يبقى معلقاً حتى تنفيذ قبول الهاتف ببيانات المستخدم.

## استعادة Windows

تمت مراجعة `restore-local-db.ps1` سطراً بسطر. السكربت يطلب ملف SQL، يتحقق من المسار واسم قاعدة البيانات، يطلب كلمة التأكيد الحرفية `RESTORE`، ثم يستخدم معاملات PowerShell آمنة نسبياً مع `mysql` لاستبدال قاعدة البيانات واستيراد النسخة. لا توجد بيئة PowerShell محلية متاحة في sandbox، كما أن تشغيل أمر DROP على قاعدة البيانات المتصلة سيكون اختباراً تدميرياً غير مناسب دون نسخة وبيانات اعتماد وبيئة Windows يملكها المستخدم؛ لذلك لم يُدّعَ تنفيذ الاستعادة الفعلية هنا.

## الحقول المالية في عمليات العقد

أضيفت في لوحة التشغيل وفي وحدة الحسابات خانات المبلغ، وطريقة الدفع (كاش أو شبكة) عند تسجيل الدفعة، وخيار مستقل لإضافة الرسوم. الرسوم الإضافية ترفع إجمالي العقد مباشرة، ويُعرض المتبقي من الإجمالي ناقص المدفوع في لوحة العملية وبطاقة تسجيل الدفعة وصفحة تفاصيل العقد. أضيف خيار تعديل سعر التأجير في إنشاء العقد وفي تشغيل عقد قائم؛ يحفظ السعر الجديد ويعيد احتساب إجمالي الوحدات مع إبقاء الإضافات السابقة، ويسجل العملية بتاريخها وتفاصيلها.

نتيجة التحقق الأخيرة: `pnpm check` ناجح، `pnpm test --run` ناجح بعدد 20 ملفاً و65 اختباراً، و`pnpm build` ناجح مع تحذير حجم الحزمة فقط. تم تطبيق migration لتعداد عمليات العقد الجديدة دون إسقاط جداول أو بيانات.

إضافة تحقق API الأخير رفعت التغطية إلى **66 اختباراً ناجحاً ضمن 20 ملفاً**، مع اختبار مباشر لتمرير عمليتي `additional_fee` و`rate_update` إلى طبقة الحفظ.

## Drill-down الجديد

تم ربط بطاقات لوحة التشغيل بالعقود السارية والمتأخرة والمحاسبة والأسطول. أصبح رقم العقد رابطاً إلى `/contracts/:id`، وتعرض صفحة العقد تاريخ الفتح والدفعات وطريقة كل دفعة والملاحظات والتأخير والمتبقي وسجل العمليات. أضيفت صفحة `/vehicles/:id` لعرض اللوحة والماركة والموديل وسنة الصنع والعداد الحالي وعداد وتاريخ آخر تغيير زيت والعقود التاريخية وسجل الصيانة. كما أضيف ملف `/customers/:id` للعقود والدفعات والتعاملات، وربطت صفوف السيارات والعملاء بملفاتها.

أضيفت ثلاثة أزرار مالية مباشرة في تشغيل العقد: **دفعة كاش** و**دفعة شبكة** و**رسوم إضافية**، مع بقاء المبلغ والمتبقي والملاحظات ضمن نفس المسار.

نتيجة الاختبار الأخيرة: `pnpm check` ناجح، و`pnpm test --run` ناجح بعدد **21 ملفاً و68 اختباراً**، كما نجح الفحص البصري على iPhone بحجم 375×812.

## فحص حقول السيارة والعداد والوثائق — 2026-08-28

تم فحص مسارات الرئيسية والسيارات وتفاصيل السيارة والعقد الجديد والحسابات على مقاس iPhone 375×812 بارتفاع كامل. ظهرت شاشة السيارات باتجاه RTL مع جدول داخل حاوية تمرير، وظهرت شاشة الحسابات مع حقول الدفعة وطريقة الدفع وقراءة العداد الحالية الاختيارية والملاحظات. عند عدم وجود جلسة محلية ظهرت شاشة الدخول المحلية بصورة سليمة؛ لذلك يبقى إظهار بيانات سيارة محفوظة فعلياً ضمن جلسة قبول المستخدم اليدوية.

## مطابقة التصور المرفق وبيانات الأسطول

أصبحت السيارة تحتفظ بالعداد الحالي، وعداد وتاريخ آخر تغيير زيت، وفترة تغيير الزيت بالكيلومترات، وتواريخ انتهاء التأمين والفحص الدوري والاستمارة. تحسب صفحة السيارة المسافة المتبقية للصيانة القادمة، وتعرض العقد والعميل الحاليين والعقود التاريخية وسجل الصيانة وكل عمليات السيارة وملاحظاتها. تقبل صفحة السيارة تعديل هذه البيانات، كما تقبل نماذج إنشاء العقد والدفعة وتشغيل العقد قراءة عداد جديدة، وتمنع طبقة الحفظ تراجع القراءة عن العداد الحالي وتضيف القراءة إلى السجل المؤرخ.

أضيفت تنبيهات داخلية للوثائق المنتهية أو التي ستنتهي خلال 30 يوماً وللسيارات التي بلغت موعد تغيير الزيت. توسعت لوحة التشغيل لتعرض إجمالي السيارات والمتاحة والمؤجرة والعقود السارية والمتأخرة وإجمالي المستحقات ودفعات اليوم وعدد حالات الوثائق والصيانة. أصبح ترتيب جدول السيارات ثابتاً حسب رقم السجل، ويعرض العميل الحالي. كما توسع جدول العقود ليعرض نوع العقد، والأيام المتبقية أو المتأخرة، والمتبقي السابق المشتق من آخر دفعة، والمتبقي الحالي.

تم تطبيق migration غير مدمرة `0006_marvelous_steel_serpent.sql` لتواريخ الوثائق و`0007_careless_bushwacker.sql` لفترة تغيير الزيت. تمنع قاعدة الحفظ تراجع قراءة العداد عند التعديل أو فتح عقد أو تسجيل عملية، وتحتسب موعد الزيت من الفترة الخاصة بكل سيارة. آخر تحقق: TypeScript ناجح، وVitest ناجح بعدد **23 ملفاً و75 اختباراً**، والبناء الإنتاجي ناجح مع تحذير حجم الحزمة فقط. لم تظهر أخطاء في أحدث سجلات الخادم أو المتصفح أو طلبات الشبكة بعد الفحص.

## إصلاح صلاحية تعديل السيارة

أظهرت صورة الاختبار رسالة `You do not have required permission (10002)` عند حفظ العداد والوثائق؛ كان سببها أن المسار `vehicles.update` يتطلب دور المدير رغم ظهور نموذج التعديل للمستخدم التشغيلي. عُدل المسار ليقبل أي مستخدم مصادق عليه لتحديث العداد والزيت وتواريخ الوثائق. وبناء على ملاحظة المستخدم بأن إضافة السيارة لا تعمل، أتيح إنشاء السيارة أيضاً للمستخدم التشغيلي المصادق عليه، مع بقاء الحذف محصوراً بالمدير. أضيفت اختبارات صريحة لهذا الفصل. 

## الصيانة والحذف والتأخر والإجراءات السريعة

زر **تسجيل صيانة** يفتح الآن نموذجاً فعلياً لا رسالة إعلامية فقط. يتضمن النموذج السيارة، ووصف الصيانة أو العطل، وتاريخ البداية، والتكلفة، والحالة، والملاحظات. عند الحفظ ينشأ سجل صيانة حي وتتحول السيارة إلى حالة صيانة، فلا تصبح متاحة لعقد جديد حتى استكمالها. كما أضيفت قائمة صيانة مباشرة تعرض التكلفة والحالة والملاحظات المحفوظة.

أضيف زر **حذف السيارة** في ملف السيارة للمدير فقط، مع رسالة تأكيد. الحذف مسموح للسيارة غير المرتبطة بأي عقد أو صيانة فقط؛ أما إذا وُجد تاريخ تشغيلي فيرفض النظام الحذف برسالة عربية حتى لا تضيع الحسابات والسجلات.

تتحول العقود النشطة التي تجاوزت تاريخ التسليم تلقائياً إلى حالة متأخرة عند تحميل لوحة التشغيل أو قائمة العقود أو التنبيهات. يحسب العرض عدد الأيام ورسوم التأخير بحسب القاعدة المعتمدة: سعر اليوم × أيام التأخر للعقد اليومي، والسعر الشهري ÷ 30 × أيام التأخر للعقد الشهري. كما تدخل مستحقات العقود المتأخرة ضمن إجمالي المبالغ القائمة في لوحة التشغيل.

اقتصرت إجراءات لوحة التشغيل السريعة على **استلام مبلغ، تمديد عقد، تبديل سيارة، إغلاق عقد، واسترجاع سيارة**. حُذفت التعليق وتعديل السعر والرسوم الإضافية من قائمة الإجراءات السريعة فقط، دون حذفها من السجل التاريخي أو طبقة البيانات. آخر تحقق: `pnpm check` ناجح، وVitest ناجح بعدد **25 ملفاً و79 اختباراً**، و`pnpm build` ناجح مع تحذير حجم الحزمة فقط. كما تمت مراجعة لقطات مسارات الرئيسية والصيانة والسيارات والعقود المتأخرة وملف السيارة في جلسة مستخدم فعلية.

## السجل المالي الحي وسجل صيانة السيارة

تستخدم شاشة **الحسابات والإيرادات** الآن `payments.list` مباشرة بدلاً من صفوف توضيحية ثابتة. يظهر لكل دفعة التاريخ والوقت، ورقم العقد، واسم العميل، وطريقة الدفع (كاش أو شبكة أو تحويل)، والمبلغ، والملاحظات. عند حفظ دفعة جديدة تُحدّث قائمة الدفعات وملخص الحسابات وتقرير إيرادات السيارات مباشرة. تُظهر صفحة **سجل المدفوعات** المنفصلة المعلومات نفسها مع رابط إيصال PDF.

أضيفت إلى سجلات الصيانة قراءة العداد ونوع الخدمة. عند اختيار **تغيير زيت** يحفظ النظام السجل بالتاريخ والعداد والتكلفة والملاحظات، ويحدّث تلقائياً `آخر تغيير زيت` و`عداد آخر تغيير زيت` في ملف السيارة. أما الصيانة العادية فتُسجل بالعداد نفسه وتضع السيارة تحت الصيانة. تظهر جميع هذه التفاصيل في صفحة السيارة وفي جدول الصيانة العام.

طُبقت migrations غير مدمرة: `0008_bored_fallen_one.sql` لإضافة عداد سجل الصيانة و`0009_cute_phalanx.sql` لإضافة نوع السجل. آخر تحقق: TypeScript ناجح، وVitest ناجح بعدد **26 ملفاً و80 اختباراً**، والبناء الإنتاجي ناجح. كما راجعت لقطات iPhone لمسارات الحسابات وسجل المدفوعات والصيانة وملف السيارة.

## السيارة المتاحة بعد تعليق العقد

أصبح **تعليق العقد** يحرر السيارة فوراً ويجعلها قابلة للاختيار في عقد جديد، مع الاحتفاظ بالعقد المعلّق وكامل سجله المالي والتشغيلي. لا تزال العقود السارية والمتأخرة فقط تمنع السيارة من الظهور في قائمة السيارات المتاحة، كما تمنع حالة الصيانة غير المكتملة إتاحتها. لا يعرض جدول السيارات أو ملف السيارة العقد المعلّق كعقد أو عميل حالي بعد تحريرها.

التحقق الأخير: `pnpm check` ناجح، واختبارات أثر العمليات وتوفر السيارة نجحت، والبناء الإنتاجي ناجح. تمت مراجعة واجهة قائمة السيارات والعقود المعلّقة على شاشة iPhone.
